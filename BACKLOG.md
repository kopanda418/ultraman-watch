# BACKLOG.md

改善候補・既知の課題を溜めておく場所。対応したら「対応済み」に移し、
何をどう変えたか一行で残す（詳細はコミットログを見れば分かるので書かない）。

## 未対応

（いまは空）

## 対応済み

- [x] `supabase/schema.sql` に UID が平文で入っていて、公開リポジトリに追跡されていた → UID を `ultraman_watch_members` 表へ移し、`schema.sql` には空の表を作る指示だけを残した。中身を入れる SQL は `internal/`（追跡外）に置く。メンバー表は行レベルセキュリティで全部拒否にし、判定の関数だけが `security definer` で覗く。過去のコミットに残る UID は、履歴の書き換えもアカウントの作り直しも行わない方針（A案。依頼者と確認済み）

- [x] 記事に対してコメントを残せるようにしたい → 1つの記事に何件でも積める形にした（古い順・誰がいつ書いたか付き）。カードは操作ボタンの下に1行足して、件数と直近の1件を見せる。押すと全画面のコメント欄が開く。2人で共有し、相手が書いた瞬間に自分の画面へ反映される。自分の書き込みだけ消せる（DB は2人のどちらからも消せる。表示名は権限の判断材料にできないため）。**Supabase の SQL Editor で `supabase/schema.sql` を流す作業が必要**（`comments` の名前が同居先で未使用なことは anon キーで実地確認済み）

- [x] 日程を手直しすると記事が目の前から移動して見失う → 並び順は仕様どおりのまま、**保存したあと移動先まで自動でスクロールし、そのカードの縁を3秒だけ光らせる**ようにした。あわせて画面下に「この位置へ移りました」と数秒だけ知らせる。絞り込みの条件から外れて一覧から消えた場合は、その旨を知らせる。実データで測ると、日程未定の記事に日付を入れると 51 番目 → 17 番目（34件ぶん）飛んでいた

- [x] 記事のタイトルを文字で検索できるようにする（インクリメンタルサーチ）→ 絞り込みパネルの一番上に検索欄を置き、打つそばから絞るようにした。見出しだけを見る（本文まで見ると狙った記事が埋もれるため）。検索する側とされる側の両方で字を揃えるので、「うるとらまん」でも「ウルトラマン」に、「ＴＨＥ　ＬＩＶＥ」でも「the live」に当たる。タグ・日程と重ねて使え、絞り込みの件数バッジにも数えられる

- [x] 情報収集を毎日11時半に実施する → cron を `30 2 * * *`（＝11:30 JST）にし、`RUN_EVERY_N_DAYS` を 1 にした。事前に上限を調べたが抵触するものは無し（Actions は無料・無制限、Supabase は収集1回で読み取り2回、リポジトリ容量は年30MB程度）。実質の変化は通知が最短で毎日になることだけで、依頼者と確認済み。cron 側の「毎日起動」は崩していない（月またぎで連続実行になるため）

- [x] フィルタリングの機能を整理する → タグを「押すたびに 絞る → 除外 → 指定なし」の3状態にし、いくつでも重ねられるようにした。除外ができたので「関東以外」タグは廃止。公式・ニュース・グッズ・セールは同時に成り立たないので、この3つだけ同じ組にして or で合わせる（他は and）。未読・ピックの列も同じタグの仕組みに統合した。日程は別枠にして、対象の日付（イベント開始日／終了日／記事の作成日）を選び、日数をスライダーと数値入力で自由に決める形にし、選んだ内容を日本語の一文で画面に出すようにした。日程不明のものは日数では消さず、「日程不明」タグで絞る／除外する。**利用者が実機で動作を確認済み（2026-08-05）**

- [x] pick済の記事をフィルタリングできるようにする → 絞り込みに4列目「ピック問わず／ピック済み／ピックしていない」を足した。未読・既読と同じ作りなので、タグ・日程・未読と重ねて使える（例: 関東かつ未読かつピックしていない）。ピックのタブでは効かせない（全部ピック済みなので、選んだ瞬間に空になってしまうため）

- [x] 二重に開く件が直っていなかった（体感9割で再発）→ 前回の「経過時間を見る」修正は**戻ってきた瞬間の誤発動**にしか効いておらず、実際の主因は別だった。`x-safari-` を叩いてから Safari が前に出るまで1〜2秒あり、その間このアプリはまだ前面で普通に動いている。一度きりのタイマーはその**立ち上がり途中の姿**を見て「効かなかった」と読み違えていた（＝待ち時間をどこに置いても、早ければこちらに、遅ければ凍結側に当たる）。点で見るのをやめ、0.25秒ごとの見張りで**3秒ずっと前面で動き続けたときだけ**保険を出す形にした。あわせて一度スキームが効いた端末は覚えておき、以後は保険自体を出さない。判定は純粋な関数（`judgeSafariTick`）に切り出し、5通りを机上で確認。**実機で解消を確認（2026-08-05）**。`internal/pwa-external-browser-answer.md` も同内容に差し替え済み
- [x] タブの件数を絞り込み後の数にする → 絞り込みまで済ませた一覧を返す `filteredFor` を切り出し、表示する中身と件数の両方をそこから採るようにした。食い違いようがない作りになっている。3つのタブすべてが絞り込み後の数になる（254→15→2→254 を確認）
- [x] タグの位置が揃わず視認性が悪い → タグを本文の情報（媒体・日付・地域）から切り離し、独立した1行にまとめた。並びは常に 未読→新着→関東→手直し→誰が で固定。高さも18pxに揃えて目で追う線を1本にした。色は役割ごとに分け、未読と手直しが同じ金色だったのも直した（手直しは灰色）
- [x] カードの操作ボタンが2段になる → アイコン＋短い文字（ピック／アーカイブ／元記事）にして幅を3等分し、1段に収めた。390px 幅で1行・高さ38px・はみ出しなしを確認。折り返しは切ってあるので、文字を長くすると再発する
- [x] ヘッダーでアーカイブと絞り込みが重なる → 390px 幅で測ると、タブが 285px 必要なのに 208px しか無く **77px はみ出していた**。原因は「絞り込み ▾」と「×」が 139px を占めていたこと。絞り込みをモノクロのアイコン（40px角＋件数は右上に重ねる）にし、解除はパネルの中へ移した。はみ出しは 0 になり、解除の誤タップの原因も消えた
- [x] Google News API の `sort_by` / `filter` が使えるか → **使えない**。searchapi.io は Google 公式ではなく第三者の有料サービスで、その独自仕様だった。RSS に同名のパラメータを付けても効かない。決め手は対照実験で、**でたらめなパラメータと `&scoring=n` の挙動が完全に一致**した（差はキャッシュが外れて別スナップショットが返るだけ）。`&filter=0` は結果が1件も変わらず。取りこぼしを減らす手段は期間を割ることだけ、という結論は変わらない。詳細は `DECISIONS.md`
- [x] また過去の記事（2014年）が入った → `when:60d` は正しく効いていた。盲点は**媒体が日付を打ち直す写真ページ**。「＜画像4 / 10＞…」で始まる walkerplus の画像ビューアで、中身は2014年や2009年なのに RSS 上の公開日は直近1週間になっており、期間指定でも公開日の判定でも防げない。見出しでこの型を落とすようにした（9件除外、walkerplus の正常な記事7件は残る）。先頭の「＜」が無い形も実在したため NFKC で字を揃えてから判定する
- [x] Safari で開いた後に戻ると同じページが二重に開いている → **待ち時間の問題ではなかった**。iOS は背面に回った PWA の JavaScript を凍結するため、Safari が出るとタイマーが止まり、戻ってきた瞬間に発火する。そのときは「前面」なので保険が走ってしまう（700ms→900ms にしても再発）。判定を `needsFallback` に切り出し、前面かどうかに加えて「一度でも背面に回ったか」と「タイマーが予定どおりの時刻に動いたか＝実際の経過時間」の3つを見るようにした。あわせて `internal/pwa-external-browser.md` の方式に揃え、iOS 判定を足し（PC/Android では発動しない）、リンク1本ずつではなく document で1回受ける形にし、`download` 属性と http(s) 以外のリンクは素通しする
- [x] 開催日をユーザが編集可能に → カードの開催日を押すとカレンダーが開く。1回目のタップで開始日、2回目で終了日、3回目でやり直し。開始日だけなら1日だけの予定になる。手直しは Supabase の `event_dates` で2人共有し、収集側も `edited_event_dates` ビューから読んで常に自動の読み取りより優先する（渡さないと次の収集で元に戻り、日付を入れた記事が保持期間の判定で消えることもある）
- [x] 元記事を外部ブラウザ(Safari)で開く → `x-safari-https:` スキームで Safari を開きにいく。Apple 非公開の仕組みで iOS 16 では効かない報告があるため、700ms 待って画面が残っていたら普通に開き直す二段構えにした。設定から丸ごと切ることもできる。**実機での確認は未実施**。詳細は `DECISIONS.md`
- [x] 設定を別画面へ移設／未サインイン時はトップにサインイン画面 → ヘッダー右の歯車から開く全画面の設定に、アカウント・表示名・通知・ピックの控え・情報源をまとめた。未サインイン時は一覧の手前にサインイン欄（メール・パスワード・表示名）を出す。**一覧は未サインインでも見られるまま**にした（`DECISIONS.md`「認証層を足す利点が薄い」を維持。依頼者と確認済み）
- [x] 記事に"未読"⇔"既読"のタグを追加 → 既定は未読で、タイトルか「元記事を開く」から飛べば既読になる。絞り込みは3列目として独立させ、タグ・日程と重ねて使える（例: 関東かつ未読）。共有しない指定なので端末内の `localStorage` に持つ。バッジは未読のときだけ出す（全件に付くと目印にならないため）
- [x] 絞り込みを閉じても結果が消えないようにする／効いている間は色で知らせる → 調べた結果、閉じても結果は消えておらず、色（グレー→ほぼ黒）も既に変わっていた。実体は**解除の「×」が 24px 角で「絞り込み」のわずか 4px 隣**にあり、閉じるつもりで誤タップして解除していたこと。当たり判定を 40px 角、間隔を 8px に広げた。あわせて効いている間は面も塗って気づきやすくした
- [x] 左スワイプで記事を archive へ送る → カードを二層にして指に追従させ、88px 以上引いて離すと成立する。縦の動きが上回るときはスクロールとみなして手を引き、ボタンやリンクの上から始めたスワイプは無視する。アーカイブタブでは同じ操作で一覧に戻る。**実機（iPhone のタッチ操作）での確認は未実施**
- [x] 300件の上限超過分は archive から優先的に削除 → アーカイブ済みを先に落とし、足りなければ通常の項目を落とす。どちらの組でも落とす順番は従来どおり並び順の末尾から。収集側は記事IDだけを返す読み取り専用ビュー `archived_ids` から読む（万能キーは使わない。同居する別アプリのデータを危険にさらさないため）。詳細は `DECISIONS.md`
- [x] "新着"と"これまで"のタブをやめ、新着はタグ（絞り込み）に。代わりに archive タブを新設 → タブを「一覧／ピック／アーカイブ」にした。アーカイブはピックと同じく Supabase で2人共有（`archives` テーブル。記事の中身も持つのは、上限超過で `items.json` から消えても中身を見られるようにするため）。**Supabase の SQL Editor で `supabase/schema.sql` を流す作業が必要**
- [x] 検索結果が上限（100件）に張り付いたとき、上限を超えて取れないか → `&num=` も `&start=` も無視されページングは不可。`after:`/`before:` で期間を割るのが唯一の手段だった。調べると**本当の制約は上限ではなく**、Google が類似記事をまとめて代表1件しか返さないことで、上限に達していなくても取りこぼしていた（単発72件 → 8分割284件）。15日刻みの4分割で取り、区間が上限に張り付いたらそこだけ半分に割って取り直すようにした。取得は51件→約210件に増加。副作用で無関係な記事（パズドラ・名探偵コナン等）が44件混入したため、検索結果に限り関連語の判定を掛けて除外した。詳細は `DECISIONS.md`

- [x] 本文の日付が「⽉」「⽇」（部首の異体字）だと読めない → `detectEventPeriod` の入口で NFKC 正規化するようにした。副産物として全角数字（`１２月２９日`）も読めるようになった（従来は取りこぼしていた）

- [x] 新着情報に過去の記事が流れてくる → 原因は重複判定の漏れではなく収集元。Google ニュースの検索 RSS は関連度順で返る件数に上限（実測100件）があり、期間を絞らないと13年前の記事まで枠に入る。枠の中身は日をまたぐと入れ替わるため、それまで一度も返ってきていなかった古い記事が毎回「新着」として流れ込んでいた。検索クエリに `when:60d` を付けて収集を60日以内に限定し、あわせて開催日不明の記事の保持期間を1年→90日に短縮した。副次効果として、枠を占領していた古い記事が消えて直近記事の取得数が増え（60日以内が24件→65件）、これまで取りこぼしていた71件を新たに拾えた。詳細は `DECISIONS.md`「検索クエリには `when:60d` を付ける」
- [x] X(旧:twitter)の検索結果から情報を引けるか → **やらないと決めた**。`x.com/robots.txt` は名前を挙げた検索エンジン以外に `Disallow: /` の全面禁止で、本ツールはそこに当たる。公式APIの検索は無料枠に無く月200ドル規模。詳細は `DECISIONS.md`「X（旧 Twitter）からは収集しない」
- [x] Google News の記事をタイトル検索で辿る（円谷公式以外の媒体）→ **今回は見送り**。検索APIの鍵（Brave / Google Custom Search など）と GitHub Secrets の管理が要るうえ、一般サイトは本文の型がバラバラで日付の誤読リスクが高い。対象は約100件。詳細は `DECISIONS.md`「円谷公式以外の媒体は、いまは辿らない」
- [x] 収集元に円谷イマジネーションを追加 → RSS が無く一覧も DOM に出てこない（Next.js 製）ため、HTML に埋まっているサーバー描画データから記事一覧を取り出す `kind: 'imagination'` を足した。イベント情報とグッズ情報の2カテゴリを収集。円谷ステーションとの重複は既存の重複判定でそのまま潰れた（40件取得 → 20件が重複として落ちた）
- [x] Google ニュース経由の記事の開催日（媒体が円谷公式の分）→ RSS の `<source url>` で媒体を見て、円谷公式なら公式サイトの検索API（`/wp-json/wp/v2/event`、robots 許可・鍵不要）にタイトルを投げてイベントページを引き当て、そこから開催期間を読むようにした。あわせて「開催期間」の読み取り範囲が広すぎて次のラベルの日付まで拾っていたのを直し、補完済みの日付も毎回読み直すようにした（誤りが残り続けないように）。開催日ありが 24 → 27 件、終了済みと判明した7件が落ちた
- [x] ヘッダーにタブと絞り込みをまとめる → ヘッダーを `position: sticky` にしてタブと絞り込みを内包し、スクロール中も切り替えられるようにした。絞り込みのチップは既定でたたみ、「絞り込み」ボタンで開閉する。効いている間はボタンに件数が付き、隣の × で一括解除できる。条件名までボタンに出すとタブを押し潰したため、件数表示に留めた
- [x] 年月日の年を取り違える（例:「12月29日開催」の記事が2025年のものなのに2026年と読まれる）→ 年が省略された日付に実行時の現在年を当てていたのが原因。記事の公開日を基準に読むよう直した（記事より前の月なら翌年と解釈）。古い記事のイベントが未来の予定に化けて `isStale` をすり抜けており、2016年の記事が「2026年11月開催」として残っていた。あわせて保存済みの項目もいまのルールで解釈し直すようにしたので、今後この手の読み取り修正は過去分にも遡って効く
- [x] PWA でタグ・日程による絞り込み → タブの下にチップを2列（タグ／日程）置き、それぞれ単一選択で絞り込めるようにした。タグを増やすときは `app.js` の `TAGS` に `{ id, label, match }` を1行足すだけでよい（日程側は `DATE_FILTERS`）。ピックは Supabase 側に `category` を持たせていないため、手元の一覧から補って絞り込んでいる

- [x] 表示順が分かりにくい → 確認の結果、並び替えロジック自体は「関東優先 → 開催日昇順 → 新着順」で既に正しく動作していた（`normalize.mjs` の `sortForDisplay`）。分かりにくく見えていた実体は下の開催日未取得の多さだったため、コード変更はせず次項で対応
- [x] 開催日「未定」が多い → 円谷ステーション（m-78.jp/event/）の記事ページは「開催期間」ラベルの直後に日付が入る一定の型だったので、タイトル・概要に日付が無い場合だけ記事本文を取得して補完するようにした（`collect.mjs` の `enrichMissingDates`）。他ソースは構造がバラバラで確度が低いため対象外
- [x] 収集元一覧を利用者に見せる → `collect.mjs` が毎回 `docs/data/sources.json` を書き出し、PWA の「情報源」欄（設定セクション最下部）に一覧表示するようにした
- [x] Google ニュース経由の記事の開催日を本文から取れないか（実施可能かの検討依頼）→ **やらないと決めた**。当初「元記事URLに到達できないので不可能」と結論したがこれは誤りで、有志が公開しているデコード手法（記事ページから署名を抜き、内部エンドポイントに POST する）で技術的には到達できる。採らない理由は robots.txt がそのパスを許可していないこと。詳細は `DECISIONS.md`「Google ニュースの記事URLはデコードしない」
  代わりに調査中に見つかった取りこぼしを直した: 既報の項目は `newItems` に入らないため、あとから開催日が分かっても「日程未確定」のまま固定される作りだった。今回の収集で日付が取れていれば既存分にも反映するようにし、開催日ありが 13 件 → 28 件に増えた
- [x] 終わったイベント・古い記事が混ざる → `isStale`（`normalize.mjs`）で収集時に除外するようにした。開催日が分かるものは終了日（無ければ開始日）が今日より前なら除外、開催日が無いものは記事の公開日が1年以上前なら除外。記事の公開日は RSS の `pubDate` が全件取れていたのでメタ情報のスクレイピングは不要だった。あわせて `detectEventPeriod` を追加し、「6月5日〜11月30日」のような期間ものの終了日も拾えるようにした（開催中のイベントを終了扱いにしないため）
- [x] 開催日の表示順を降順に → `sortForDisplay` の日付比較を反転（関東優先は維持）。過去のイベントは収集時に除外するため、先頭には「一番先の予定」が来る
- [x] 「新着」がいつ外れるか分かりにくい → 仕様は「直近の収集バッチと一致する間」。バッチIDが日付単位（`YYYY-MM-DD`）だったため同日に2回収集すると新着件数が合算される副作用があったのを、実行ごとに一意なタイムスタンプに変更して修正。あわせてPWA上に「新着」の意味を説明する注記を追加
