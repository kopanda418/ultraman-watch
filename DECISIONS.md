# DECISIONS.md

決定事項とその理由。覆すときは理由ごと書き換えること。

## 実行基盤: GitHub Actions + GitHub Pages

常時稼働のサーバーが不要で、収集ジョブと通知送信を同じワークフローに同居させられるため。Cloudflare Workers も候補だったが、構成要素が増える。

## リポジトリはパブリック

GitHub Free で Pages を使うため。プライベートリポジトリからの Pages 公開はPro 以上が必要で、かつ**公開されたサイト自体は結局パブリック**（サイトの非公開化は Enterprise Cloud 限定）。秘密情報は Secrets に隔離されており、パブリックでも露出しないため、Free で十分と判断した。

ただし収集データは全部読まれる前提。ピック情報だけは端末内の IndexedDB に留め、リポジトリにはコミットしない。

## 利用者は2名（本人と配偶者）

通知先は Secrets の `PUSH_SUBSCRIPTIONS` に配列で持つ。1台が失効してももう1台には届くよう、送信は個別に評価する。

サイト自体は URL を知っていれば誰でも見られる状態のままとする。扱う情報はすべて公開されているイベント情報であり、認証層を足す利点が薄いため。

## ピックは2人で共有する。保存先は Supabase

静的サイトからは書き込めないため書き込み先が要る。Cloudflare Workers + KV も検討したが、依頼者が既に Supabase + Vercel の構成を複数運用しているため、管理コストを増やさない Supabase を採用した。

PostgREST が API を自動生成するのでサーバーコードは書かない。認証は Supabase Auth のメール+パスワードにする。
マジックリンクは iOS の PWA だと Safari 側で開いてしまい戻ってこられないため、パスワード方式を選んだ。

anon キーは公開前提のキーなのでコミットしてよい。守るのは RLS の役目。

### RLS は「authenticated なら誰でも」ではなく UID を明示指定

Supabase の無料プランのプロジェクト上限に達したため、既存の別プロジェクトと同居する形を取った。
同居先には既に本人・配偶者のアカウントがあるが、`authenticated` ロール全体を許可する設計のままだと
「anon キー経由で誰かが自分でサインアップし、authenticated になって picks を読み書きする」余地が残る。
そのため RLS ポリシーは `auth.uid()` を本人・配偶者の2つの UID に明示的に絞った関数
（`is_ultraman_watch_family()`、`supabase/schema.sql`）越しに許可する形にしている。
UID 自体は非公開情報として扱い、リポジトリには含めていない（`internal/` 参照）。

### schema.sql が同居先に触れないことを、実地で確かめてある

同居しているため、`public` スキーマに物を置くたびに名前がぶつかる恐れがある。
`if not exists` は「既にあれば作らない」だけで、その後ろの
`enable row level security` や `create policy` は**既存の表に当たってしまう**。
相手の表と同じ名前を使うと、相手の設定を書き換えることになる。

そこで貼る前に、作る名前が既にあるかを anon キーで1つずつ叩いて確認した
（2026-08-04 実施）。`archives` / `event_dates` / `archived_ids` /
`edited_event_dates` はいずれも未使用で、衝突しないことを確認済み。
**名前を増やすときは、同じ確認を先に行うこと。**

同居先には `profile_docs` がある。中身は見ていない。

共有物に触れるのは `supabase_realtime` パブリケーションだけ。表を足すのみで
他アプリの分は消さないため影響しない。ただし既に入っている表をもう一度足すと
エラーで実行が止まるので、貼り直せるよう重複だけ握りつぶしてある。

### Auth のプロジェクト共通設定は変更しない

同居先の既存プロジェクトが Auth の「Confirm email」を OFF にした状態に依存しているため、
ultraman-watch 側からこの設定を含む Auth 共通設定（サインアップ許可、パスワードポリシー等）を
変更しない。テーブル単位で分離できない設定のため、変更が必要になった場合は同居先への影響を
先に確認すること。

## 通知: Web Push を主系

追加アプリ不要で、Safari 第一優先という要件に最も素直。LINE Notify は2025年3月末で終了済みのため候補から除外した。
メール送信は堅牢だが、まず Web Push で運用してみて、取りこぼすようなら並走させる。

## cron は毎日起動、1日おきの判定はスクリプト側

cron の `*/2` は月をまたぐと連続実行日が発生するため。

## 自治体イベントはニュース検索 RSS で拾う

市町村サイトを個別に巡回するのは維持コストが高すぎる。Google ニュースの検索 RSS を複数クエリで回すほうが網羅性・安定性ともに勝る。
クエリは OR で束ねず、目的別に分けて投げる（取りこぼしが減る）。

### 検索クエリには `when:60d` を付ける（60日以内に限定する）

Google ニュースの検索 RSS は「新着順」ではなく**関連度順**で、返る件数に上限がある
（実測で 100 件）。期間を絞らないと 13 年前の記事まで枠の中に入る。しかも枠の中身は
日をまたぐと入れ替わるため、それまで一度も返ってきていなかった古い記事が
「初めて見る記事」＝新着として毎回流れ込んでくる。

実測（2026-08-04）:

- 同じ日に連続で叩くと中身は完全に一致する。秒単位ではぶれない
- 上限 100 件のうち 60 日以内の記事は **24 件**しかない。古い記事が枠を占領していた
- 4 クエリ計 327 件のうち、既報で弾いたのが 312 件、URL 張り替えをタイトル類似で
  弾いたのが 12 件、**完全に初見が 3 件**。その 3 件の公開日は 0 日前・821 日前・5699 日前
- `when:60d` を付けると、60 日以内の記事を **65 件**拾えるようになる

つまり重複判定の不具合ではなく、収集元が古い記事を混ぜて返していたのが原因。
期間を絞ると流入が止まるうえ、枠を圧迫していた古い記事が消えて**直近の取得数が増える**。

### 対象期間は15日ずつに割って取る（上限を超えて拾うため）

`&num=` も `&start=` も無視されるためページングはできない。**期間を割ることが
上限を超える唯一の手段**（`after:` / `before:` は効く。実地確認・2026-08-04）。

そして本当の制約は 100 件の上限ではなかった。Google は**類似記事をまとめて
代表 1 件しか返さない**ため、上限に達していなくても取りこぼしている。
実測（「ウルトラマン イベント」の 60 日分、重複判定後の件数）:

| 取り方 | リクエスト | 生 | 重複判定後 |
|---|---|---|---|
| 単発 `when:60d` | 1 | 72 | 51 |
| 15日刻み 4分割 | 4 | 166 | — |
| 7.5日刻み 8分割 | 8 | 284 | 210 |

細かくするほど増え続けるが、増える分は日程未定・関東外に偏り雑音も混ざる。
量と精度の釣り合いで **15 日刻み**を採った（依頼者と確認済み）。
加えて、ある区間が上限に張り付いたらそこだけ半分に割って取り直す（`fetchNews`）。
深さと1ソースあたりのリクエスト数で歯止めをかけ、際限なく叩かないようにしている。

### `sort_by` や `filter` は使えない（RSS は q 以外を見ない）

「`sort_by=most_recent` で新着順にでき、`filter` で重複を抑えられる」という
情報があるが、これは **searchapi.io という第三者の有料サービス**の仕様で、
Google 公式の API ではない。同名のパラメータを RSS に付けても効かない
（2026-08-04 実測）。

| 付けたもの | 件数 | 基準との差 |
|---|---|---|
| 同じ URL を2回（対照） | 66 → 66 | 0 件 |
| でたらめな `&nonsense_param=xyz123` | 69 | 5 件 |
| `&scoring=n` | 69 | 5 件 |
| `&tbs=sbd:1` | 70 | 7 件 |
| `&filter=0` | 67 | 0 件 |

**存在しないパラメータと `&scoring=n` の挙動が一致した**のが決め手。差は
パラメータの効果ではなく、URL が変わってキャッシュが外れ、別のスナップショットが
返るだけ。日付順に並ぶものも一つも無かった。

つまり RSS が見ているのは `q`（`when:` `after:` `before:` などの検索演算子を含む）と
言語・地域の指定だけ。**取りこぼしを減らす手段は期間を割ることしかない**という
上の結論は変わらない。同じ調査を繰り返さないよう、対照実験の形ごと残しておく。

### 検索結果には関連性の判定を掛ける

Google は検索語をゆるく解釈するため、分割して件数を増やすと無関係な記事も
一緒に増えた（実測でパズドラ・名探偵コナン・大昆虫展・ものまねライブなど、
293 件中 44 件）。日程未定かつ関東だと一覧の上のほうに出てしまい、目的を損ねる。

そこで**検索結果に限り**、ウルトラマン関連と分かる語が見出しにも概要にも
無いものは捨てる（`NEWS_RELEVANT`）。公式サイト由来の記事には掛けない。
「S.H.Figuarts …」のように語が出ない見出しがあり、公式である時点で関連は
保証されているため。実測では公式ソースに該当は 0 件だった。

判定は保存済みの項目にも遡って掛ける。掛けないと、判定を入れる前に取り込んだ
無関係な記事が保持期間いっぱい居座る。

### Google ニュースの記事URLはデコードしない

Google ニュース RSS の `link` は `news.google.com/rss/articles/...` の難読化された
中継URLで、元記事のURLではない。これをデコードする手法は有志が公開しており
（例: SSujitX/google-news-url-decoder）、技術的には成立する。
記事ページから署名とタイムスタンプを抜き、`/_/DotsSplashUi/data/batchexecute` という
Google の内部エンドポイントに POST して元URLを得るというもの。

**それでも採用しない。** `news.google.com/robots.txt` は `Disallow: /` が基本で、
許可されているのは `/`, `/home`, `/topics/`, `/publications/`, `/stories/`, `/swg/`,
`/about` だけ。この手法が叩く `/articles/` と `/_/...` はどちらも許可されていない。
加えて内部エンドポイントは公開契約が無く、予告なく壊れる。

この結論により、ニュース検索経由の記事は**開催日を本文から取れない**。
タイトルと概要から取れた分だけが `eventDate` を持つ。ここは仕様として受け入れる。

### 媒体が円谷公式なら、タイトルから公式サイトの記事を引き当てる

Google ニュース RSS の item には媒体を示す `<source url>` が付く。元記事の URL は
分からないが、どのサイトの記事かは分かる。媒体が m-78.jp（円谷ステーション）なら、
公式サイトの検索 API（`/wp-json/wp/v2/event`）にタイトルを投げれば同じイベントの
ページに辿り着ける。robots.txt が許可している公開 API で、鍵も要らない。
開催日はそのページの「開催期間」から読む。

同名のイベントページが年ごとに残っているため、タイトルの一致度が並んだら新しい
ほうを採る。取り違えると、終わったイベントの日付が付いてしまう。

### 円谷公式以外の媒体は、いまは辿らない

同じことを一般の媒体でやるには検索 API の鍵（Brave / Google Custom Search など）が
要り、GitHub Secrets の管理が増える。加えて一般サイトは本文の型がバラバラで、
日付の誤読リスクが高い。対象は約 100 件あるが、費用対効果が見合わないと判断して
見送った（2026-08-04、依頼者と確認済み）。やるなら鍵の取得から。

### 検索 RSS 自体は使い続ける

上記の robots.txt は `/rss/` も許可リストに挙げていない。厳密にはグレーだが、
RSS は Google が自動取得される前提で発行している配信の仕組みであり、
フィードを読むことと、記事ページを巡回して内部APIを叩くことは性質が違う、と判断した。
依頼者に論点を説明したうえで、このまま使う方針を確認済み（2026-08-03）。

## X（旧 Twitter）からは収集しない

`x.com/robots.txt` は名前を挙げた検索エンジン以外（`User-agent: *`）に対して
`Disallow: /` の全面禁止で、本ツールはこの `*` に当たる。Google ニュースの記事
ページを巡回しないと決めたのと同じ理由がそのまま当てはまる。

公式 API の検索機能は無料枠に無く、有料プラン（月 200 ドル規模）が要る。
2 人で使う趣味のツールには見合わないため採らない（2026-08-04、依頼者と確認済み）。

## 地域: 全国を収集し、関東を優先表示

除外はしない。並び順で関東を先頭に寄せ、カードに「関東」バッジを付ける。

## 終わったイベント・古い記事は収集の時点で落とす

見たいのは「これから行けるもの」だけなので、過ぎたものは残さない（`isStale`）。
開催日が分かるなら終了日（無ければ開始日）が今日より前で判定し、
開催日が分からないものは記事の公開日が古すぎるかどうかで代用する。
公開日すら無いものは判断材料が無いので残す。

代用の閾値は当初1年にしていたが、**90日に短縮した**（2026-08-04、依頼者と確認済み）。
1年では一覧 188 件中 160 件が開催日不明で、うち 97 件が公開から 60 日超という
「古い告知だらけ」の状態になり、依頼者の目的（効率よく直近の情報を得る）を損ねていた。
下の収集窓（60日）より少し広く取り、告知から開催まで間があるものを拾える余地を残している。

短縮の代償として、開催中でも日付が読めなかった長期イベントを数件落とす。
同じイベントは複数の媒体が別記事で報じるため、実害は小さいと判断した。

期間もののイベントを開始日だけで判定すると開催中のものまで消えるため、
`detectEventPeriod` で終了日も拾う。年が省略された「6月5日〜11月30日」も、
直前の年を引き継いで解釈する。

## 並び順: 関東優先 → 開催日の降順

開催日は「先の予定ほど上」にする。過去のイベントは収集の時点で除外しているため、
昇順にしても降順にしても残るのは未来の予定だけで、依頼者の希望で降順を採った。
開催日が取れなかったものは日付ありの後ろに置き、その中では新しく見つかった順。

## 保持は 300 件まで。溢れた分は並び順の末尾から落とす

`KEEP_ITEMS`（`collect.mjs`）。初回コミットから入っていた値で理由が記録されて
いなかったため、現行のまま据え置くと決めたうえでここに残す（2026-08-04、依頼者と確認済み）。

**上限が効いている先は 2 つ。** どちらも件数にそのまま比例する。

- `docs/data/items.json` は収集のたびに git へコミットされる。無制限だとリポジトリが際限なく膨らむ
- PWA は起動のたびに `items.json` を全件ダウンロードする（`app.js`、キャッシュ無効化付き）。
  iPhone のモバイル回線で毎回読む前提なので、素直に軽いほうがよい

実測で 1 件あたり約 1.1KB。250 件で 276KB、300 件で約 0.32MB、800 件なら約 0.86MB。
1MB 前後が体感の分かれ目と見て、300 件はその手前に収まっている。

**溢れたときに落ちるもの。** まず**アーカイブしたものから落とす**。
「もう見た・興味がない」と本人が判断したものなので、残す価値が一番低い。
アーカイブ分で足りなければ、続けて通常の項目を落とす。
どちらの組の中でも落とす順番は上の並び順の末尾から、すなわち
「関東外 かつ 日程未定 かつ 見つけたのが古い」ものからになる。
関東のものと開催日が分かっているものは、アーカイブしていない限り上限では落ちない。

落としたあとに全体の並び順へ戻すのは、アーカイブを解除したときに
その記事が本来の位置に戻るようにするため。

**アーカイブの読み取り方。** 収集は GitHub Actions で動くため、PWA 上の操作を
そのままでは知れない。`archives` 本体は行レベルセキュリティで2人に閉じたままにし、
**記事 ID だけを返す読み取り専用ビュー `archived_ids`** を anon に開けて、そこから読む
（2026-08-04、依頼者と確認済み）。service_role キーを GitHub Secrets に置く案は採らなかった。
この Supabase プロジェクトは別アプリと同居しており、万能キーが漏れると相手のデータまで
危険にさらすため。ID の列だけなら表示名も記事の中身も外に出ず、書き込みもできない。

ビューが読めなくても収集は止めない。落とす順番がアーカイブを考慮しない
従来どおりに戻るだけで、収集そのものは成立するため。

**落ちた項目は戻らない。** `state/seen.json` には既報として残り続けるため、
次の収集で同じ記事が返ってきても既報として弾かれ、`items.json` に再登場しない。
「一時的に隠れる」ではなく「消える」動作である点に注意。
上限に達するまでは発生しない（2026-08-04 時点では未到達）。

## 元記事は `x-safari-https:` で Safari を開きにいく

ホーム画面から起動した PWA では、普通のリンクはアプリ内ブラウザ
（SafariViewController）で開き、Safari 本体には渡らない。Apple はこれを
制御する公式の手段を用意していない。

`x-safari-https:` は Safari を開くための URL スキームで、`https:` を置き換えて
使う。Apple が公開している仕組みではなく、iOS 15 / 17 / 18 では効き、
iOS 16 では効かないという報告がある。そこで二段構えにした。

1. まずスキームで開こうとする。成功すれば画面が Safari に移る
2. 効かなかったと分かったときだけ、普通に開き直す

**問題は 2 の判定で、一度きりのタイマーでは決められない。**
どこに置いても、次の二方向のどちらかに外れる。

| 外し方 | 中身 | 早い判定 | 遅い判定 |
|---|---|---|---|
| A | Safari が前に出るまで1〜2秒あり、その間このアプリはまだ前面で動いている | **当たる** | 当たらない |
| B | iOS は背面の PWA の JavaScript を凍結する。止まったタイマーは戻った瞬間に発火し、そのときは当然「前面」 | 当たらない | **当たる** |

2026-08-04 に B だけを塞ぎ（経過時間を見て凍結を検知する形）、実機では
**A が主因で体感9割再発した**。凍結される前に発火してしまえば経過時間は
そのままなので、B 向けの手当ては A に効かない。**待ち時間の調整でも直らない。**

そこで、点ではなく線で見る形にした（`watchSafariLaunch` / `judgeSafariTick`）。

- 0.25 秒ごとに見張り、**3秒ずっと前面で動き続けたときだけ**「効かなかった」とする（A 対策）
- 途中で背面に回るか、前回の見張りからの間隔が飛んでいれば（＝止められていた）
  そこで打ち切って保険を出さない（B 対策）
- **一度スキームが効いたのを見届けた端末は `localStorage` に覚え、以後は保険自体を出さない**

2026-08-05 に実機で解消を確認。判定は純粋な関数に切り出してあるので、
触るときは `judgeSafariTick` のコメントにある表がそのまま検証になる。

手法の詳細と移植手順は `internal/pwa-external-browser.md` と、その訂正版
`internal/pwa-external-browser-answer.md`。**元メモのコード例（前面判定だけ）と
訂正版の一度目（経過時間を見る）は、どちらもこの不具合が出る。**
横取りは iOS の standalone のときだけ行い、`download` 属性の付いたリンクと
http(s) 以外は素通しする。

公開された仕様ではない以上、将来 Apple の都合で黙って効かなくなり得る。
そのときアプリ内ブラウザで開くところまで落ちるだけで済むよう、
必ずこの落とし口を残しておくこと。

加えて、設定から丸ごと切れるようにしてある。手元で確かめられない不具合が
出たときに、コードを直さなくても元の挙動に戻せるようにするため。

## 重複判定は3段構え

1. 正規化 URL の SHA-1（計測パラメータを除去してから）
2. タイトル正規化キー + 開催日
3. 文字バイグラムの Jaccard 類似度 0.75 以上

複数媒体が同じイベントを別の見出しで報じるため、URL だけでは足りない。
