# HANDOFF.md

次のセッションが最初に読む資料。「いま何ができていて、次に何をすべきか」だけを書く。
決定の理由は `DECISIONS.md`、改善候補は `BACKLOG.md`、作業の経緯は `git log` にある。

## このプロジェクトは何か

ウルトラマン関連のイベント・特売情報を1日おきに自動収集し、iPhone の Safari
（ホーム画面に追加した PWA）で閲覧できるようにするツール。
新着があれば Web Push で通知する。利用者は本人と配偶者の2名で、通知は両方に届く。

## 現在地

**初期構築は完了し、すでに自動運転に入っている。** 毎日 06:00 JST に起動し、
1日おきに実収集する。新着があれば2台の iPhone に通知が飛ぶ。

| 状態 | 内容 |
|---|---|
| 完了 | 収集スクリプト（実サイトで検証済み。全ソースが RSS ベース） |
| 完了 | GitHub リポジトリ・Actions・Pages |
| 完了 | Supabase（既存プロジェクトに相乗り。RLS は UID 直指定） |
| 完了 | Web Push（VAPID 鍵・2台分の購読情報とも Secrets 登録済み） |
| 完了 | 通しテスト（実際に2台へ通知が届くところまで確認済み） |
| 完了 | 終了済み・古い記事の除外、開催日の降順表示、タグ／日程の絞り込み |

- 公開 URL: https://kopanda418.github.io/ultraman-watch/
- リポジトリ: https://github.com/kopanda418/ultraman-watch

## 最初にやること

`BACKLOG.md` の「未対応」を上から。`/backlog` スキルで進められる。
セッションを閉じるときは `/handoff`。

いまの未対応は収集元を増やす話が2件。どちらも着手前に確認したい論点がある。

1. **円谷イマジネーションをソースに追加**。円谷ステーションと内容が重なるため、
   重複表示をどう防ぐかが本題。重複判定は3段構えで効いているが、同じイベントを
   別サイトが別見出しで出すので、類似度の閾値調整が要るかもしれない
2. **X（旧 Twitter）から拾えるか検討**。API が有料化されているので、
   まず「無料の範囲で取れるのか」の調査から。取れないなら早めにそう伝える

依頼が無いときにこちらから提案するなら、次の2点が弱い。

- **開催日が取れている項目が少ない**（173件中24件）。残りは大半がニュース検索経由で、
  後述のとおり本文を見に行けない。タイトルに「2026年4月」のように月までしか
  書かれていない取りこぼしが十数件あるので、拾うなら精度と引き換えの判断になる
- **関東の項目に開催日付きがほぼ無い**。「関東優先 → 開催日の降順」で並べているため、
  先頭は関東・日程未確定の項目が占める。並びの意図が利用者に伝わりにくい可能性がある

## 触る前に知っておくべき設計上の癖

- **`docs/` に秘密情報を置かない。** Pages で全世界に公開される。秘密鍵に触れるのは
  `scripts/notify.mjs` だけで、Actions の Secrets 経由でしか渡していない。この分離を壊さない
- **`internal/` は公開されない**（`.gitignore` 済み）。他プロジェクトとの申し送りなど、
  リポジトリに載せたくないものはここに置く。逆に、この中身を `HANDOFF.md` などに
  書き写さないこと
- **cron は毎日起動する。** 1日おきの判定は `scripts/collect.mjs` 冒頭の剰余計算。
  `*/2` は月をまたぐと連続実行になるため意図的にこの形にしている
- **重複判定は3段構え。** URL ハッシュ → タイトル+開催日のキー → バイグラム類似度 0.75。
  `state/seen.json` が正。ここを消すと既報が全部再通知される
- **`detectRegion` の実装順序に依存がある。** 「東京都」に「京都」が含まれるため、
  正式名称を先に消してから略称を探している。この順序を入れ替えない
- **既報の項目は `newItems` に入らない。** そのため後から開催日が判明しても、意識して
  書き戻さないと取り込んだ当時のまま固定される。いまは `collect.mjs` の `carried` で、
  保存済みの項目も毎回いまのルールで解釈し直している。おかげで日付の読み取りを直せば
  過去分にも遡って効く。項目に後付けする情報を足すときは同じ罠に注意
- **年が省略された日付の基準は「記事の公開日」。** 「12月29日開催」のような表記に
  実行時の現在年を当てると、古い記事のイベントが未来の予定に化けて `isStale` を
  すり抜ける（実際に2016年の記事が「2026年11月開催」で残っていた）。
  `detectEventPeriod` の第2引数には必ず `publishedAt` を渡すこと
- **Supabase は別プロジェクト（travel-ai）と相乗りしている。** Auth の設定は
  プロジェクト共通なので、「Confirm email」等を変更すると相手側が壊れる。触らないこと。
  RLS は `authenticated` 全体ではなく UID 直指定にしてある
- **ニュース検索経由の記事は本文を見に行けない。** Google ニュースの `link` は難読化
  された中継URLで、デコード手法は存在するが robots.txt が許可していないため使わない
  （`DECISIONS.md` 参照）。「日付が取れないから元記事を取得しよう」という発想で
  同じ道を再発見しないこと
- **ヘッドレスブラウザは入れない方針。** カードゲームの大会情報ソースは Nuxt 製 SPA で
  静的HTMLから中身が取れず、依存を増やさない判断でソースごと削除した経緯がある
- **PWA アイコンは iOS がホーム画面追加時の値を保持する。** 差し替えても、削除して
  追加し直すまで反映されない。購読情報も同時に作り直しになるので、アイコンを変えたら
  `PUSH_SUBSCRIPTIONS` の更新もセットで考える

## よく使うコマンド

```powershell
npm install
$env:FORCE="true"; node scripts/collect.mjs   # 日付判定を飛ばして収集を即実行
npx serve docs                                 # PWA をローカルで確認
gh workflow run collect.yml                    # Actions を手動実行（実際に通知が飛ぶ）
```

`npm run collect` は `FORCE=true` の指定が cmd.exe 経由だと通らないため、
PowerShell では上記のように環境変数を分けて渡すこと。
